#encoding: utf-8
<div class="large-4 columns texto-limitado">
  <h2 class="titulo-gerando-assunto">Gerando assunto</h2>
  <ul class="trending-topic">
    <% @episodios_top.each do |episodio| %>
        <li>
          <span class="comentarios-armazenados"><%= episodio.comments.count %> </span><a href="/episodios/<%= episodio.slug %>"><%= episodio.nome_episodio_formatado %></a>
        </li>
    <% end %>
  </ul>
</div>